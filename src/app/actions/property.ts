"use server";

export async function createPropertyAction() {
  return { error: "Demo mode — listings are seeded." };
}

export async function updatePropertyAction() {
  return { error: "Demo mode — listings are seeded." };
}

export async function deletePropertyAction() {
  return { error: "Demo mode — listings are seeded." };
}
